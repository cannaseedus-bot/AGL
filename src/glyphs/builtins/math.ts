export const math = {
  add: (a: any, b: any) => Number(a) + Number(b),
  subtract: (a: any, b: any) => Number(a) - Number(b),
  multiply: (a: any, b: any) => Number(a) * Number(b),
  divide: (a: any, b: any) => Number(a) / Number(b)
};
